apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-pvc
  labels:
    app: mongo
spec:
  accessModes:
    - ReadWriteOnce 
  volumeName: mongodb-volume
  resources:
    requests:
      storage: 2Gi
status: {}